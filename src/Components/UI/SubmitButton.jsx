import Button from "@mui/material/Button";
export default function SubmitButton() {
  return (
    <Button
      variant="outlined"
      size="large"
      color=""
      loading={false}
      // endIcon={<SendIcon />}
    >
      Submit
    </Button>
  );
}
